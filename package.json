{
  "name": "base-project",
  "version": "1.0.0",
  "description": "Base project with backend, frontend, and shared packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=packages/backend",
    "dev:frontend": "npm run start --workspace=packages/frontend",
    "build": "npm run build --workspace=packages/backend && npm run build --workspace=packages/frontend",
    "build:frontend": "npm install --workspace=packages/frontend && npm install --workspace=packages/shared && npm run build --workspace=packages/shared && npm run build --workspace=packages/frontend",
    "install:all": "npm install && npm install --workspace=packages/backend && npm install --workspace=packages/frontend && npm install --workspace=packages/shared"
  },
  "devDependencies": {
    "@types/estree": "^1.0.8",
    "@types/jsonwebtoken": "^9.0.10",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "@reduxjs/toolkit": "^2.8.2",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^7.6.2",
    "react-select": "^5.10.1",
    "sonner": "^2.0.5",
    "tailwind-merge": "^3.3.1"
  }
}
